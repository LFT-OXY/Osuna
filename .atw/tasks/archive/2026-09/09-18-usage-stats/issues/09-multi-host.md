# 09 — 多主机：合并、主机筛选与不可用主机标记

**What to build:** 连接多台主机时「用量」页默认合并全部已连接且支持的主机；总览头部出现主机筛选下拉（一台主机时不显示）：首项「全部主机」带「N 台计入」pill，正常主机绿点可选，旧版本主机灰字不可选 + 「需要更新主机」pill，未连接主机灰点不可选 + 「未计入」pill；选中某台后各区块只显示该主机；来源卡片不按主机再拆。

**Status:** ready-for-agent
**Impl:** done

**Blocked by:** 08

依据：spec 实现决策第 9 节主机筛选与数据获取、第 13 节；08 号决策票跨 Host 合并一节；12 号票跨切面。

- [x] 接缝 3：两台真 daemon（参照现有双主机 spec），各自根目录指向**各自一份**夹具树——两份内容相同，于是「两台之和」就是单机值的整二倍，断言不依赖第二份夹具的具体数字；来源卡片按 `(cli, backend)` 合并（份额不变）；下拉里选第二台后数字变为其单机值。
- [x] 接缝 3：断开第二台后下拉标「未计入」且合计减少；连接一台旧 daemon（现有旧 daemon 夹具）后标「需要更新主机」且不计入。
- [x] 接缝 3：只连一台主机时头部无筛选控件。
- [x] `usage.hostFilter.*` 键 9 语言，运行时拼键处 `i18n.exists()` 用例；资源测试通过。
- [x] `npm run typecheck`、`npm run lint`，改动的测试与 Playwright spec 通过。
