# 访谈决策记录（2026-09-26）

来源：/atw-askme 访谈，全部为用户明确回答。

## 已定

| # | 决策 | 结论 |
|---|---|---|
| 1 | 上游合并 | 不再合并 getpaseo/paseo，UI 可以动布局 |
| 2 | 平台 | 以 Electron 桌面端为主验收；移动端跟随 token 变化，不单独设计 |
| 3 | 迁移方式 | 渐进：先改 token → 补 Typography / Row 等基础组件 → 逐页迁移；每一步可单独验证、回滚 |
| 4 | 主参考 | t3code 为主，multica 补充（见 reference-t3code.md、reference-multica.md） |
| 5 | 中间区域结构 | 保留 Osuna 现有 workspace 多 tab + 分屏，只换外观（tab 条、pane 头、分隔线对齐 t3code 风格）；右侧 Explorer 对齐 t3code 右面板样式。不改成 t3code 的"对话固定居中 + 右面板 + 底部终端抽屉" |
| 6 | 毛玻璃 / 噪点等质感 | 只在 Web / Electron 开启；原生降级纯色 |
| 7 | 现有 20 套主题 | 新亮 / 暗作为默认；旧主题用公式从旧色派生新增语义角色，自动映射，不逐套手调 |
| 8 | 字体 | 系统字体栈 |
| 9 | 原型 | 先做原型再动代码 |
| 10 | 原型范围 | 桌面工作区完整一屏（左侧栏、tab 条、对话流：用户消息 / 助手文本 / 工具调用 / 思考 / 代码块、composer、右侧 Explorer 的 Changes 或 diff）+ 设置页（行式卡片）+ 一个菜单或对话框浮层。不做移动端原型 |
| 11 | 原型形态 | 单个本地 HTML，静态假数据，可切亮 / 暗，左侧栏可切几种样式；放仓库外临时目录，不进 git、不发布线上 |
| 12 | 原型左侧栏 | V1 现状换皮（36px 单行 + ±diff，第二行分支 / PR）、V2 t3code 三行（约 78px）、V3 orca 卡片 + 内嵌 agent 行，三种都做，一键切换 |
| 13 | 原型附加开关 | 暗色侧栏比主区亮（orca）/ 暗（t3code）；V2 中间行用工作区名 / agent 标题 |

## 原型结论

左侧栏 V1 + 借 V3 选中描边与未读加粗；暗色侧栏比主区暗；详见 prototype.md。discover 阶段无未决问题。
